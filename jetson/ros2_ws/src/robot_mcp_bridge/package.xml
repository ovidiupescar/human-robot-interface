<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>robot_mcp_bridge</name>
  <version>0.1.0</version>
  <description>
    Long-lived daemon bridging the ROS2 graph to Hermes Agent.
    Owns the rclpy node (runs in system Python 3.10), exposes ROS2 actions as
    MCP tools over HTTP+SSE for Hermes (Python 3.11), and pushes perception
    events to the platform adapter over a WebSocket.
  </description>
  <maintainer email="ovidiu@artoriuslabs.com">Ovidiu Pescar</maintainer>
  <license>MIT</license>

  <depend>rclpy</depend>
  <depend>std_msgs</depend>
  <depend>robot_face_msgs</depend>
  <depend>robot_perception_msgs</depend>
  <depend>robot_control_msgs</depend>
  <depend>robot_graph_msgs</depend>

  <export>
    <build_type>ament_python</build_type>
  </export>
</package>
