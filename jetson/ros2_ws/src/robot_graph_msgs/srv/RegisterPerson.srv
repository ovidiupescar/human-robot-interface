# Bind a name to the current/most-recent speaker (or to provided audio).
string name
uint8[] audio_pcm_int16     # optional; if empty, use most recent voice sample
uint32 sample_rate
---
PersonIdentity identity
bool success
string message
